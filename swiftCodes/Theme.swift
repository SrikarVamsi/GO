import SwiftUI

// Global theme colors for the app
struct Theme {
    static let primaryColor = Color(red: 0.2, green: 0.6, blue: 0.4)
    static let secondaryColor = Color(red: 0.1, green: 0.4, blue: 0.3)
    static let accentColor = Color.white
}

